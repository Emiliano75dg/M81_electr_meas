sample_id: demo-sample
instruments:
  m81:
    enabled: true
    connection:
      kind: mock
  daq6510:
    enabled: true
    resource: MOCK::DAQ6510
    settle_s: 0.05
  environment:
    kind: mock
    mode: integrated
    endpoint: http://localhost:8000
output:
  directory: data
logging:
  level: INFO
